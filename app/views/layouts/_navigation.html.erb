<div class="nav-container">
  <nav class="navbar navbar-expand-md fixed-top navbar-light ">
    <%= link_to image_tag("kitboxer_simple_logo.png", width: 110, style: 'margin: 0 30px 5px 0'), root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse">
      <span class="nav-divider d-none d-lg-block"></span>
      <ul class="navbar-nav">
        <li class="nav-item ml-2 mb-3 mr-2 " style="">
          <% if user_signed_in? %>
              <%= link_to cart_path class: 'nav-link' do %>
                  <i class="fa fa-shopping-cart  nav-icon" style=" position: relative;top: 9px">
                    <span class="badge badge-cart"><%= current_user.cart_count if current_user.cart_count > 0 %></span>
                  </i>
              <% end %>
          <% end %>
        </li>
        <li class="nav-item ml-4 mb-3 mr-2" style="">
          <i class="fa fa-globe nav-icon" style="position: relative; top: 9px">
          </i>
        </li>
      </ul>
      <span class="nav-divider d-none d-lg-block"></span>
      <%= form_tag(products_path, method: :get, class: 'form-inline') do %>
          <i class="material-icons" style="color: #51646B">search</i>
          <input type="text" class="nav-search form-control" placeholder="Search kits...">
      <% end %>
      <ul class="navbar-nav ml-auto">
        <%= render 'layouts/nav_links_for_auth' %>
      </ul>
    </div>
  </nav>
</div>
