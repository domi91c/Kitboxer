<div class="purchase-conversation__message
                <%= receipt.sender == current_user ? 'purchase-conversation__message--right' : '' %>">
  <strong><%= receipt.sender.full_name %></strong>
  <%= simple_format receipt.body %>

  <% if receipt.attachment? %>
    <strong>Attachment:</strong>
    <%= link_to receipt.attachment.file.filename, receipt.attachment.url, target: :blank %>
  <% end %>
</div>

