<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Kitboxer" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Kitboxer" %>">
  <meta name="turbolinks-cache-control" content="no-cache">

  <script src="https://js.stripe.com/v3/"></script>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
  <%= csrf_meta_tags %>
  <%= stylesheet_pack_tag 'application' %>
</head>
<body style="background-color: #fafbff;" data-environment="<%= Rails.env %>">
<%= render 'layouts/navigation' %>
<%= content_for?(:header) ? yield(:header) : "" %>
<div role="main" style="min-height: calc(100vh - 70px); margin-top:30px;">
  <div class="container-fluid">
    <%= render 'layouts/messages' %>
    <%= yield %>
  </div>
</div>
<%= render 'layouts/footer' %>
<%= hidden_field_tag :authenticity_token, form_authenticity_token -%>
<%= javascript_pack_tag 'application' %>
</body>
</html>

