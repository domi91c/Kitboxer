<div class="row">
  <div class="col-sm-12 col-sm-offset-3 col-md-6 offset-md-3">
    <div class="card">
      <div class="card-block">
        <% if resource.class.name == 'User' %>
            <h1 class="display-5 text-center">Sign Up</h1>
        <% elsif resource.class.name == 'Client' %>
            <h1 class="display-5 text-center">Borrower Sign Up</h1>
        <% end %>
        <br>
        <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), validate: true, html: {role: "form"}) do |f| %>
            <%= devise_error_messages! %>
            <div class="form-group">
              <%= f.email_field :email, placeholder: 'Email', class: "form-control", id: 'signup_email' %>
            </div>
            <div class="form-group">
              <%= f.password_field :password, placeholder: 'Password', class: "form-control", :validate => {:presence => true}, id: 'signup_password' %>
            </div>
            <div class="form-group">
              <%= f.password_field :password_confirmation, placeholder: 'Confirm password', class: "form-control", id: 'signup_password_confirmation' %>
            </div>
            <div class="form-group">
              <%= f.submit t('.sign_up', :default => "Sign Up"), class: "btn btn-outline-info pull-right" %>
            </div>
        <% end %>
      </div>

    </div>

  </div>

</div>