<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Kitboxer" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Kitboxer" %>">
  <meta name="turbolinks-cache-control" content="no-cache">

  <%= stylesheet_link_tag 'coming_soon', media: 'all', 'data-turbolinks-track' => 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
  <%= csrf_meta_tags %>

</head>
<body data-environment="<%= Rails.env %>">
<div class="coming-soon-page">
  <div class="table-center">
    <div class=" col-md-6 mx-auto">
      <%= link_to (image_tag 'logos/kitboxer_logo.png', width: 170), products_path %>
      <h1 class="display-2">Kitboxer</h1>
      <h2 class="display-5">Coming March 2018.</h2>
      <br>

      <div class="js-subscription-container col-lg-7 mx-auto">
        <%= form_with(model: Subscription.new, class: 'js-subscription-form', validate: true) do |form| %>
          <div class="error-block"></div>
          <div class="input-group">
            <%= form.text_field :email, class: 'coming-soon-form-control form-control', placeholder: 'Email', validate: { precense: true } %>
            <span class="input-group-btn">
             <%= form.submit 'Subscribe', class: 'btn btn-primary coming-soon-btn' %>
          </span>
          </div>
        <% end %>
      </div>
      <br>
    </div>
  </div>
</div>
</body>

</html>
