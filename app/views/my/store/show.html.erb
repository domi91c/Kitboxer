<%= render 'store_header' %>

<section class="store-show container mt-5">
  <br>
  <div class="card-deck">
    <% @products.each do |product| %>
      <%= render 'products/product', product: product %>
    <% end %>
  </div>
  <%= link_to 'New Product', products_path, method: :post %>
</section>

