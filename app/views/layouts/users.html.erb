<% content_for :header do %>
  <div class="page-header">
    <br>
    <br>
    <div class="container-fluid">
      <div class="user-info text-center">
        <%= image_tag 'default_avatar.png', width: 100, class: 'rounded-circle' %>
        <br>
        <h1 class="mt-3">Dominic Nunes</h1>
        <div class="card-social-media-buttons d-inline-block mt-3">
          <span><i class="card-social-media-button fa fa-facebook"></i></span>
          <span><i class="card-social-media-button fa fa-twitter"></i></span>
          <span><i class="card-social-media-button fa fa-google"></i></span>
        </div>
      </div>
      <ul class="nav nav-tabs page-header-tabs justify-content-center" role="tablist">
        <li class="nav-item">
          <%= link_to "Orders", user_orders_path(current_user),
                      class: "nav-link #{current_class?(user_orders_path(current_user))}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Reviews", user_reviews_path(current_user),
                      class: "nav-link #{current_class?(user_reviews_path(current_user))}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Watching", user_favorites_path(current_user),
                      class: "nav-link #{current_class?(user_favorites_path(current_user))}" %>
        </li>
      </ul>
    </div>
  </div>
<% end %>
<%= yield %>
<%= parent_layout("application") %>
